export class SeatType {
  // id: string;
  // name: string;
  // description: string;
  // quantity: number;
  // price: number;

  constructor(public id: string,
              public name: string,
              public description: string,
              public quantity: number,
              public price: number) {
  }
}
